# Telegram-бот для подготовки к экзамену

Telegram-бот для подготовки к экзамену с режимами тестирования и проверкой знаний по 60 вопросам.

## 📋 Описание

Бот предназначен для подготовки к экзамену путем решения тестов с выбором одного правильного ответа. Пользователь может выбрать один из двух режимов тестирования и получать мгновенную обратную связь о правильности ответа.

## 🚀 Основные функции

### Режимы тестирования:

1. **Сплошная сессия** 📝
   - Прохождение всех 60 вопросов последовательно (с 1 по 60)
   - Автоматический переход к следующему вопросу после ответа
   - Мгновенная обратная связь после каждого ответа

2. **Выбор вопроса** 🔍
   - Возможность выбрать любой вопрос из 60 доступных
   - Удобная клавиатура с номерами вопросов (1-60)
   - Возможность вернуться к выбору другого вопроса

### Функциональность:

- ✅ Мгновенная проверка ответов
- ✅ Отображение правильного ответа при ошибке
- ✅ Удобная навигация с кнопками
- ✅ Возможность вернуться в главное меню в любой момент
- ✅ Асинхронная работа для быстрого отклика

## 🛠 Технологии

- **Python 3.8+**
- **aiogram 3.4.1** - асинхронная библиотека для работы с Telegram Bot API
- **python-dotenv 1.0.0** - для работы с переменными окружения

## 📦 Установка и запуск

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка токена бота

Создайте файл `.env` в корне проекта (если его нет) и добавьте токен бота:

```
BOT_TOKEN=ваш_токен_бота
```

**Важно:** Файл `.env` уже добавлен в `.gitignore` и не будет загружен в репозиторий.

### 3. Запуск бота

```bash
python src/main.py
```

## 📁 Структура проекта

```
t-bot-questions/
├── data/
│   └── QUESTIONS_BASE.json    # База вопросов (60 вопросов)
├── src/
│   └── main.py                # Основной код бота
├── .env                       # Токен бота (не коммитится)
├── .gitignore                 # Игнорируемые файлы
├── requirements.txt           # Зависимости проекта
└── README.md                  # Документация
```

## 📊 Формат данных

Вопросы хранятся в файле `data/QUESTIONS_BASE.json` в следующем формате:

```json
{
    "question1": "Текст вопроса",
    "answers": [
        "Вариант ответа 1",
        "Вариант ответа 2",
        "Вариант ответа 3"
    ],
    "correct_answer": "Правильный вариант ответа"
}
```

Вопросы пронумерованы от `question1` до `question60` и идут строго по порядку.

## 🎮 Использование

1. Запустите бота командой `/start` в Telegram
2. Нажмите кнопку "🚀 Начать решать"
3. Выберите режим тестирования:
   - **Сплошная сессия** - для последовательного прохождения всех вопросов
   - **Выбор вопроса** - для работы с конкретными вопросами
4. Выберите ответ на вопрос
5. Получите мгновенную обратную связь:
   - ✅ "Молодец! Правильный ответ!" - при правильном ответе
   - ❌ "Ошибочка!" + правильный ответ - при неправильном ответе

## 🔐 Безопасность

- Токен бота хранится в файле `.env`, который не коммитится в репозиторий
- Файл `.env` добавлен в `.gitignore` для защиты конфиденциальных данных

## 📝 Основные компоненты кода

### Функции загрузки данных:
- `load_questions()` - загрузка вопросов из JSON файла
- `get_question_by_number()` - получение вопроса по номеру
- `get_question_key()` - формирование ключа вопроса

### Функции создания клавиатур:
- `create_answer_keyboard()` - клавиатура с вариантами ответов
- `create_question_selection_keyboard()` - клавиатура выбора вопроса (1-60)
- `create_main_menu_keyboard()` - главное меню

### Обработчики:
- `/start` - команда запуска бота
- `start_test` - начало тестирования
- `mode_continuous` - режим сплошной сессии
- `mode_select` - режим выбора вопроса
- `handle_answer` - обработка выбранного ответа
- `back_to_menu` - возврат в главное меню

## 🔄 Состояния (FSM)

Бот использует Finite State Machine (FSM) для управления состояниями:
- `TestStates.waiting_for_answer` - ожидание ответа пользователя
- `TestStates.question_selection` - выбор вопроса

## ⚙️ Особенности реализации

- Полностью асинхронный код для оптимальной производительности
- Использование Inline-клавиатур для удобной навигации
- Автоматическая обработка длинных текстов ответов (обрезание до 50 символов в кнопках)
- Поддержка HTML-разметки для форматирования сообщений
- Память состояний через MemoryStorage

## 📞 Поддержка

При возникновении проблем проверьте:
1. Правильность токена в файле `.env`
2. Установлены ли все зависимости из `requirements.txt`
3. Существует ли файл `data/QUESTIONS_BASE.json` с корректной структурой данных
